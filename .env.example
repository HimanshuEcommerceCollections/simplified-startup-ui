# Base URL of simplifiedstartup-server (no trailing slash).
# Used by the start-project form and the footer newsletter.
# Defaults to http://localhost:4000 when unset (local dev).
NEXT_PUBLIC_API_URL=http://localhost:4000
